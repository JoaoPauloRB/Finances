@using Web.Components.Modal;
@using Web.Components.Transfer

@inherits BlazorStateComponent

<Modal @ref="@modal" Title="Transferência">
  <Transfer CancelCallback="@modal.Close" SubmitCallback="@modal.Close" />
</Modal>

@code {
  public Modal modal = new Modal();
  public void Open() {
    modal.Show();
  }
}