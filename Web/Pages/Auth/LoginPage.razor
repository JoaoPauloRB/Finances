@page "/login"
@using Domain.Models
@inherits BlazorStateComponent

<EditForm Model="@user">
    <div class="form-group">
        <label>
            Email:
            <InputText type="email" @bind-Value="user.Email" class="form-control"/>
        </label>
    </div>
    <div class="form-group">
        <label>
            Password:
            <InputText type="password" @bind-Value="user.Password" class="form-control"/>
        </label>
    </div>
    <button type="submit" class="btn btn-primary">Login</button>
</EditForm>


@code {
  //AuthState State => GetState<AuthState>();
  private User user = new User();
  private async void Login()
  {    
    //await Mediator.Send(new UserState.Login(user));
  }
}