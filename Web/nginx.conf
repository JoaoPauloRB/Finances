events { }
http {
    include mime.types;
    types {
        application/wasm wasm;
    }

    server {
        listen 3000;
        listen 3001 ssl;

        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html =404;
        }
    }
}